<?xml version="1.0" encoding="utf-8"?>
<!--
  CONTROLLED AppLocker policy for the PSM (versioned with the deployment script).
  Goal: keep control of the AppLocker policy instead of applying the default one
  as-is, and include the additional binaries in it (see config\software.psd1).

  This file is a PLACEHOLDER: to be filled in at deployment time from the
  CyberArk reference PSMConfigureAppLocker (version 12.6 / 14.0), extended with
  the rules matching each application listed in software.psd1.

  TODO (deployment):
    - Start from the CyberArk template of the target version.
    - Add an Allow rule per additional installed binary.
    - Check that the paths/publishers match the real installers.
-->
<AppLockerPolicy Version="1">
  <!-- TODO: RuleCollections (Exe / Script / Dll / Msi) to define at deployment time. -->
</AppLockerPolicy>
